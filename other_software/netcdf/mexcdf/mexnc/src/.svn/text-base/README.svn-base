If you need to recompile your mex-file (R2008a and R2007b only), do the following.  On unix, type

    make           # R2008a only
    make R2007b    # R2007b only

On windows, start up matlab, type either

    >> win32_make_mexnc
or

    >> win64_make_mexnc

