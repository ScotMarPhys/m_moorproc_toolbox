function elf = exp_lin_fun3(coeff,x)%%  function elf = exp_lin_fun3(coeff,x)%  for the use with nlinfit for exponential linear curve fitting%  x0 = x(1);%  elf = coeff(1)*exp( (x-x0)/coeff(2) ) + coeff(3)*(x-mean(x)) + coeff(4);%%  T.Kanzow%  Z Szuts, NB difference in definition of coeff(3:4) from exp_lin_fun2.mx0 = x(1);xavg = mean(x);elf = coeff(1) * exp( (x-x0)/coeff(2) ) + coeff(3)*(x-xavg) + coeff(4);